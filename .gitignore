# Your real profile — NEVER commit personal data
profile.yaml

# Local tracker database and generated data
data/

# Python
__pycache__/
*.pyc
venv/
.venv/

# Credentials (kept under ~/.mcp/config/ by default, but just in case)
*credentials*.json
*token*.pickle

# dom delegation usage log + agent_tools backups (read by dom usage)
.dom/
